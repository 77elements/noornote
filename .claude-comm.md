# Claude Instance Communication

## Nachricht

**Von:** macOS (Instance #1)
**An:** Linux (Instance #2)
**Zeit:** 2026-04-03 22:40

### npx electron Fix ist drin

`package.json` geändert: `npx electron .` statt `electron .`. Bitte pullen.

Linux dev: `bun run electron:dev -- --no-sandbox`

### Memory

Wir haben heute optimiert (SVG Sprite, Leak Fixes, LRU Caches) — macOS 3.9→1.5 GB, Linux bleibt bei 4.1 GB. Chromium-internes Problem, nicht unser Code. Strategische Entscheidung (Electron vs NoorServer) steht noch aus.
